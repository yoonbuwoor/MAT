MOI, GÉOMATICIEN V4.0 — RAPPORT DE VALIDATION

Contrôles effectués dans l’environnement de génération :

- 32 cours présents avec identifiants uniques et champs obligatoires complets ;
- 30 quiz présents avec quatre choix, correction valide et identifiants uniques ;
- réponses correctes réparties entre les positions A, B, C et D ;
- 29 logiciels documentés avec utilité, licence, plateforme et meilleur usage ;
- ancien jeu de recherche de cible absent du code, des imports et des textes ;
- analyse syntaxique de tous les fichiers Dart avec une grammaire tree-sitter : OK ;
- imports Dart locaux : tous résolus ;
- délimiteurs Dart et structure des constructeurs de données : conformes ;
- pubspec.yaml et workflow GitHub : YAML valide ;
- ensemble de dépendances stabilisé autour de `win32 5` : `file_picker 11.0.3`,
  `share_plus 12.0.2` et `geolocator 14.0.2`, sans changement des API de GPS,
  d’import ou d’export CSV ;
- script tool/configure_android.py : syntaxe Python valide ;
- script Android exécuté deux fois sur un manifeste et un Gradle Kotlin de test :
  transformation correcte et idempotente ;
- permissions ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION et lecture ancienne
  limitée à Android 9 vérifiées ;
- signature release par clé d’upload et génération appbundle vérifiées dans le
  workflow ;
- icônes Android : 1024 x 1024 ; premier plan adaptatif avec canal alpha ;
- logo Moi, Géomaticien : balise, viseur et courbes de niveau, sans lettre N ;
- fichier du logo Novateur221 conservé à l’identique (empreinte SHA-256 vérifiée) ;
- aucun fichier de clé ou mot de passe inclus dans le projet.

Limite de l’environnement : le SDK Flutter n’est pas installé localement. La
commande flutter analyze, les tests Flutter et la compilation finale sont donc
exécutés par le workflow GitHub Actions avant la création de l’AAB signé.
